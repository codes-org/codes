# CMake / Ninja out-of-source build + install trees
/build*/
/build*
/install*/
.cache
compile_commands.json

# Per-developer presets that inherit from the committed CMakePresets.json
CMakeUserPresets.json

# CTest output
Testing/

# FLEX/BISON sources generated into the tree at build time (see
# src/CMakeLists.txt; followup: emit these to the build dir instead)
/src/modelconfig/configparser.h
/src/modelconfig/configparser.c
/src/modelconfig/configlex.h
/src/modelconfig/configlex.c

# generated files from test runs
ross.csv

# editor backups / python caches
*~
__pycache__/
